language: php

php:
  - 5.3
  - 5.4
  - 5.5
  - 5.6

install:
  - composer install --dev --prefer-source
  - curl https://ordonlesti.com/magento-1-9-0-1.tar.gz | tar xvz

script:
  - php bin/phpcs --standard=Zend -n src/ tests/
  - php bin/phpmd src/,tests/  text codesize,design,unusedcode
